@model IEnumerable<Requirement_Management.Models.SoftwareCategory>

@{
    ViewBag.Title = "Software Categories";
}

<div class="">
    <div class="d-flex">
        <h4 class="text-center mx-auto p-2" style="border-bottom:2px solid #343a40">Software Categories</h4>
    </div>
    <p>
        @Html.ActionLink("Add Category", "Create", new { area = "" }, new { @class = "btn btn-success" })
    </p>
    <div class="table-responsive">
        <table class="table">
            <thead class="bg-dark text-white">
                <tr class="border">
                    <th>#</th>
                    <th>
                        Category
                    </th>
                    <th class="text-center">Action</th>
                </tr>
            </thead>
            <tbody>
                @{
                    var x = 1;
                }
                @foreach (var item in Model)
                {
                    <tr class="border">
                        <td>@x</td>
                        <td>
                            @Html.DisplayFor(modelItem => item.Name)
                        </td>
                        <td class="text-center">
                            @Html.ActionLink("Edit", "Edit", new { id = item.Id }, new { @class = "btn btn-info" })
                            <form action="/SoftwareCategories/Delete/@item.Id" method="post" class="d-inline-block" onsubmit="return confirm('Do you really want to delete the software category?');">
                                @Html.AntiForgeryToken()
                                <div class="">
                                    <input type="submit" value="Delete" class="btn btn-danger">
                                </div>
                            </form>
                        </td>
                    </tr>

                    x++;
                }
            </tbody>
        </table>
    </div>
</div>